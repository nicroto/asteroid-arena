/* jshint -W098 */

var should = require("should");

describe( "asteroid-arena", function() {
	describe( "awesome()", function() {
		it( "should be awesome", function() {
			'awesome'.should.equal( 'awesome' );
		} );
	} );
} );